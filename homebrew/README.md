# Homebrew

Install Homebrew first, then run:

```sh
brew bundle --file ./homebrew/Brewfile
```

The Brewfile captures formulas and casks from the source machine. Review it on a
new machine before running if you want a smaller setup.

