# Claude-mem

This folder keeps only the settings template.

Do not migrate:

- `claude-mem.db`
- `chroma/`
- `logs/`
- `.env`
- `worker.pid`
- backup databases

The memory database is folder/session-specific state, not portable common config.

